export const signup = (_req, res) => {
  res.status(200).json({ message: "Signup is working fine" });
};

export const signin = (_req, res) => {
  res.status(200).json({ message: "Signin is working fine." });
};
